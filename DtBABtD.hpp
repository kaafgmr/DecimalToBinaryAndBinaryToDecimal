#ifndef __DTBABTD__HPP__
#define __DTBABTD__HPP__

#include <string>
#include <iostream>
#include <vector>

int VectorToInt(std::vector<int> V);

std::vector<int> IntToVector(int integer);

int DecimalToBinary(int d);

int BinaryToDecimal(int b);

#endif